import java.util.Scanner;
→ Mengimpor kelas Scanner dari library Java agar bisa membaca input dari keyboard.
Scanner digunakan untuk membaca input pengguna.
Volume dihitung dari panjang * lebar * tinggi.
Operator ternary (jarak <= 10) ? 4250 : 6000 memilih tarif berdasarkan jarak.
Jika volume > 100, maka ditambah Rp 50.000.
Output menampilkan total biaya pengiriman.
